<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext to any local network IP so the scan API works on any Wi-Fi -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
